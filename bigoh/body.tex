\typeout{(body.tex)}

\begin{frame}
    \titlepage
\end{frame}

% asymptotic growth rates
\input{asymptIdea}

% why asymptotic
    % big inputs
    % from course introduction slides
\input{whyAsmpt}

% big-omega, big-theta, big-oh 
    % + Venn diagram
\input{bigOIntuit}

% time complexity assumptions
    % measured with input sizes
    % comparing *functions*
    % comparing *worst-cases*
\input{complexFunct}

% formal definitions
% examples of formal definition
    % O
\input{formalDefn}

% consequences of formal definition
\input{formalDefnConseq}

    % f \in O(g) --> forall m > 0. f(m) < g(m)
        % counterexample
    % f \in O(g) --> there exists m > 0. f(m) < g(m) 
        % proof sketch
    % f \in O(g) --> there exists m > 0. f(m)
        % proof sketch

% informal definitions

% Big-Omega

% Big-Theta

% miscellany
    % little-oh (strictly less)
    % litte-omega (strictly more)


% the limit-based definition

% properties of limits

% aside: = notation


%---

% common complexity classes

