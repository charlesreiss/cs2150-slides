\begin{frame}[fragile,label=auto]{auto}
\lstset{language=C++,style=small}
\begin{lstlisting}
vector<int> v;
auto it = v.begin(); 
// instead of:
vector<int>::iterator it = v.begin();
\end{lstlisting}
\end{frame}
