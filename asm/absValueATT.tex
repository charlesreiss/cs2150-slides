\begin{frame}[fragile,label=absValueATT]{absolute\_value}
\lstset{
    language=myasm,
    style=smaller
}
\begin{itemize}
    \item \texttt{clang++ -S}: (AT\&T syntax)
\end{itemize}
\ldots
\begin{lstlisting}
absolute_value:
    movq    %rdi, -8(%rsp)
    cmpq    $0, -8(%rsp)
    jge    .LBB1_2
    xorl    %eax, %eax
    movl    %eax, %ecx
    subq    -8(%rsp), %rcx
    movq    %rcx, -8(%rsp)
.LBB1_2:
    movq    -8(%rsp), %rax
    retq
\end{lstlisting}
\ldots
\end{frame}
